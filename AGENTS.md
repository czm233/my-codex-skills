# 项目协作约定

## Skill 本机同步与发布

- 新增、修改或删除 `skills/` 下的任何 Skill 时，必须先同步当前电脑上的 Codex Skill 安装目录，再进行验收。仓库中的源码是维护来源，本机安装版本是实际测试对象。
- 新增或修改 Skill 时，将完整 Skill 目录同步到本机 Codex Skills 目录（默认 `~/.codex/skills/<skill-name>`），包括 `SKILL.md`、`agents/openai.yaml` 及其脚本、参考资料和资源；删除 Skill 时，也要删除对应的本机安装版本。
- 覆盖本机安装版本前，先比较仓库版本与本机版本。如果本机存在不属于本次任务的独立修改，停止覆盖并向用户说明，不能静默丢弃。
- 同步后必须核对仓库版本与本机安装版本一致；删除场景必须确认本机对应目录已经不存在。
- 必须使用本机安装版本执行至少一次符合真实调用方式的测试，不能只读取或测试仓库目录中的源码。复杂 Skill 还要覆盖审批门禁、作用域限制、失败路径等关键行为。
- Skill 安装后如需下一轮对话才能生效，应在新的对话轮次中调用本机安装版本完成测试，再判断是否通过。
- 只有本机同步完成、安装内容核对一致且实际调用测试通过后，才允许提交并推送。任一步骤无法完成或验证失败时，停止提交和推送并说明阻塞原因。

默认执行顺序：

1. 修改仓库中的 Skill。
2. 运行结构和内容校验。
3. 同步到本机 Codex Skills 安装目录。
4. 核对仓库版本与本机安装版本一致。
5. 使用本机安装版本执行真实场景测试。
6. 测试通过后提交并直接推送远端默认主分支。

## bark-notifications 多机可分发约束

- 本节只适用于 `skills/bark-notifications`，暂不扩展为其他 Skill 的通用要求。
- `bark-notifications` 必须按“可在多台 macOS 电脑独立安装和运行”的可分发 Skill 设计；任何目标电脑安装后，都应能仅依据 Skill 自带文档完成配置、验证和故障排查，不得依赖当前维护电脑、既有对话或维护者的隐含知识。
- 仓库中的脚本、Hook 示例和文档不得把 `Air`、`Mini`、具体用户名、当前电脑名称、当前用户目录或其他单机信息写成固定业务逻辑。文档中的机器名称只能使用明显的占位值或示例值。
- Bark Device Key、机器标识、实际 `CODEX_HOME`、用户目录和其他每台电脑不同的值必须留在目标电脑本地，通过统一且有明确优先级和回退行为的配置接口注入；真实本机值不得提交到仓库。
- 多台电脑向同一个 Bark 设备推送时，通知必须通过通用的机器标识机制区分来源。机器标识不得依赖预设的设备型号列表；新增电脑时应只需完成本机配置，不需要修改 Skill 源码。
- `references/codex-hook-setup.md` 必须面向不了解当前项目和对话的 Agent，完整说明：安装或更新 Skill、保存本机 Bark Key、配置机器标识、解析目标电脑的实际路径、合并且不覆盖既有 Hook、审核与信任 Hook、Dry-run、真实设备验证、故障排查以及敏感信息边界。
- 配置缺失或不可用时必须有明确、安全的回退行为；通知失败不得影响 Codex 主任务，也不得通过读取或发送对话正文来补偿配置缺失。
- 发布前除现有 Skill 校验和本机安装版真实调用外，还必须覆盖多机相关场景：两个不同机器标识可区分、未配置机器标识的回退、特殊字符与长度限制、不同用户目录或 `CODEX_HOME`，以及示例中不存在真实凭证和维护电脑专属值。
